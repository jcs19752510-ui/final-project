# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 세션을 시작할 때마다 자동으로 읽는
지속 지침 파일입니다. 절대 임의로 무시하지 마세요.

## ⚠️ Git 관련 작업 금지 (2026-09-05, 사용자 지시 — 최우선 규칙)

**Claude는 이 저장소에서 git과 관련된 어떠한 작업도 스스로 수행하지 않는다.**
`git status`/`diff`/`log` 같은 조회성 명령을 포함해 `add`/`commit`/`merge`/
`branch`/`push`/`rm` 등 상태를 바꾸는 명령까지 전부 금지 대상이다. 브랜치
생성, 커밋, 병합, 파일 삭제 등 지금까지 이 세션에서 했던 모든 git 조작
방식은 앞으로 전면 중단한다. git으로 확인해야 할 정보가 필요하면 사용자에게
직접 실행해달라고 요청하고 결과를 받아서 참고한다. 파일 내용 편집 자체
(Read/Edit/Write)는 이 규칙의 대상이 아니다 — git 명령 실행만 금지다.

## 내부 테스트 결과 및 절차 문서화 규칙 (2026-09-07, 사용자 지시)

**모든 작업 진행 이력은 이 CLAUDE.md의 "작업 이력" 섹션에 남긴다.** 세션이
바뀌어도 이 파일 하나만 보면 지금까지 무엇을 했고 왜 그렇게 했는지 알 수
있어야 한다.

**내부적으로 테스트할 수 있는 모든 기능은 실제로 테스트하고, 그 결과를
`내부테스트결과서/` 폴더에 마크다운 파일로 남긴다.** (pytest 실행, curl
스모크 테스트, docker 빌드/기동 확인, lint/type-check 등 — "코드를 작성
했다"만으로 끝내지 않고 §자기검증까지 완료한 뒤 기록한다.)
- 파일명 형식: `{작전명}_{yyyyMMddHHmmss}.md` — 작전명은 이번에 테스트한
  단위/기능을 간결하게 나타내는 한글 이름(예: `U1_인증탈퇴테스트`).
- 내용에는 최소한 (1) 무엇을 어떻게 테스트했는지, (2) AC별 pass/fail,
  (3) 발견된 문제와 조치를 포함한다.

**모든 절차적인 내용(실행 순서, 상태 전이, 데이터 흐름 등)은 mermaid
다이어그램으로 작성한다.** 표나 글로만 설명하지 말고, 순서가 있는 내용은
`flowchart`/`sequenceDiagram` 등으로 시각화해서 함께 남긴다. 이 규칙은
CLAUDE.md 자체, ADR, TRD, 작업지시서, A0 인수인계, 내부테스트결과서 등
이 프로젝트의 모든 문서에 공통으로 적용된다.

## 프로젝트 개요 (2026-09-07 갱신 — 아래 "저장소 전환 결정" 참조)

**웹 AI 모의면접 플랫폼 (MVP)** — 지원자가 웹 브라우저에서 AI 면접관과
턴 기반(녹음 버튼) 모의면접을 진행하고, 답변 내용·기술 코딩 테스트·
비언어적 지표(표정/음성 운율)를 종합한 피드백 리포트와 채용 적합도
점수를 받는 1인 개발 파이널 프로젝트입니다.

- **원본 기획서**: `00 파이널 프로젝트 계획서/*.pdf` 2건. 이 기획서는
  Kubernetes, Oracle, Pinecone, Hume AI, GCP, 500명 동시접속 부하테스트 등
  **엔터프라이즈 프로덕션급 스펙**으로 작성되어 있습니다.
- **실제 제약(2026-09-07 사용자 확인)**: 개발자 1인, 기간 4주 이내, 예산은
  Claude Code 구독료 외 전부 무료(오픈소스/무료 티어)만 사용. 이 제약 때문에
  원본 기획서의 스택/아키텍처를 대폭 축소·대체했습니다 — 상세 결정과 근거는
  `docs/adr/adr-001-mvp-architecture.md` 참조. 특히 REQ-N-001(800ms~1.5s
  실시간 스트리밍 대화)은 **턴 기반(녹음 버튼) 방식**으로 대체되었습니다.
- 하네스 원칙(아래 8개)과 Git 작업 금지 규칙은 이 프로젝트에도 그대로
  적용됩니다. 다만 1인/4주 규모에 맞춰 `harness_11`(RACI), `harness_12`
  (변경관리) 등 팀 단위 문서는 형식적으로 채우지 않고 필요할 때만
  가볍게 참조합니다 — 이 스코프 조정도 20년차 PM 판단으로 자동진행
  처리했으며 근거는 `자동진행/` 로그에 남겼습니다.
- 산출물 배치: `harness/`는 원래 목적대로 범용 프로세스 가이드로 그대로
  유지(수정하지 않음). 이 프로젝트의 실제 TRD/ADR/작업지시서/A0 인수인계는
  전부 `docs/` 하위(`docs/adr/`, `docs/trd/`, `docs/workorder/`,
  `docs/handoff/`)에 쌓습니다. 코드는 `src/`, 테스트는 `tests/`.

## 저장소 전환 결정 (2026-09-07, 사용자 승인 — 아래 2026-09-05 결정을 이 저장소에 한해 대체)

**결정**: 이 저장소(`HARNESS_BASIC` 골격)를 새 저장소를 만들지 않고 **이
저장소 자체를 파이널 프로젝트("웹 AI 모의면접") 저장소로 전환**한다.
GitHub "Use this template" 흐름을 쓰지 않은 이유는, 그 흐름이 git 저장소
생성/설정 작업(사람이 GitHub UI에서 직접 해야 함)을 전제로 하는데 이번
세션은 파이널 프로젝트를 즉시 이 대화 안에서 진행하는 것이 목적이었기
때문입니다. `harness/` 폴더 자체(21종 문서)는 원칙대로 도메인 중립을
유지하며 수정하지 않습니다 — 실제 프로젝트 내용은 전부 `docs/`·`src/`·
`tests/`에만 채웁니다.

### (참고) 2026-09-05 결정 — 원래 권장 방식, 다른 프로젝트에 이 하네스를
### 재사용할 때는 이 방식을 따르세요

이 저장소를 (전환 없이) 다른 프로젝트의 출발점으로 다시 쓰고 싶다면, 아래
2026-09-05 결정을 따르는 것이 여전히 권장됩니다: GitHub **Template
repository**로 등록해두고, 새 프로젝트마다 "Use this template"으로 완전히
새로운 저장소를 만들어 그 안에서만 실제 코드/TRD/문서를 쌓습니다.

**검토했던 대안과 기각 이유**:
- 매번 `harness/` 폴더만 수동 복사 → 간단하지만 버전 추적이 안 되고,
  이 저장소가 개선돼도 이미 복사된 프로젝트에 반영이 안 됨
- git submodule/subtree로 여러 프로젝트가 참조 → 복잡도 대비 이득이
  적음(1인/소규모 프로젝트엔 과함)

새 저장소 생성 후 할 일: 그 저장소의 `CLAUDE.md` "프로젝트 개요"를 실제
프로젝트 설명으로 교체(하네스 원칙 8개와 이 "Git 관련 작업 금지" 규칙은
그대로 유지), 이후 `harness_00_definition.md` §4 Phase A부터 순서대로
진행. 같은 내용이 `README.md`와 `harness_00_definition.md` 양쪽에도
동일하게 안내돼 있습니다.

## 하네스 원칙 (harness/harness_00_overview.md 기반, 반드시 준수)

1. **스코프 준수**: `docs/trd/`에 없는 기능을 임의로 추가하지 않는다.
2. **에스컬레이션**: 정책이 불명확하면 임의로 판단하지 말고, 진행을 멈추고
   구체적으로 무엇이 불명확한지 보고한다. (예외: 작업지시서 §3에 기본값이
   명시된 항목은 그 기본값을 따라 진행)
3. **선행 확인**: 작업 시작 전 반드시 해당 단위의 TRD(`docs/trd/`)와
   작업지시서(`docs/workorder/`)를 먼저 읽는다.
4. **자기 검증**: 작업 완료 후 TRD의 AC(§5) 각 항목에 대해 스스로
   pass/fail을 점검하고 결과를 보고한다.
5. **판단 근거 기록**: 스펙에 명시 안 된 세부사항을 채웠다면, 왜 그렇게
   채웠는지 근거를 결과 보고에 포함한다.
6. **원본 우선**: 스펙과 실제 코드/데이터가 다르면 실제 쪽을 우선하고,
   이 사실을 반드시 보고한다.
7. **개인정보 보호**: 이용자/고객 등 실제 개인정보는 실제 값 대신 항상
   샘플/마스킹 데이터만 사용한다. 실제 개인정보를 코드나 시드 데이터에
   하드코딩하지 않는다.
8. **되돌리기 어려운 결정 금지**: 데이터 모델 변경, 삭제 정책(물리삭제 등),
   권한 구조는 스스로 확정하지 말고 후보만 제시한 뒤 사람의 승인을 받는다.

## Git 워크플로우 (이 하네스를 실제 프로젝트에 적용할 때의 권장 방식)
- **기준(base) 브랜치: `PROD_SCH`** (2026-09-07 선언 — 환경상 이미 이 브랜치가
  기본 브랜치로 설정돼 있어 그대로 채택. `.github/workflows/ci.yml`의
  `on.pull_request.branches`도 반드시 이 값과 일치시킨다 — 아래 항목).
- 프로젝트 착수 시 기준(base) 브랜치를 하나만 정하고 CLAUDE.md에 명시한다 —
  선언한 기준 브랜치와 실제 push/PR 대상이 항상 일치하는지 확인한다
  (`harness_06_meta_improvement.md` §3 "기준 브랜치 선언과 실제 운영 불일치" 참조).
- 기준 브랜치에는 직접 커밋하지 않는다.
- 작업 단위마다 `feature/{단위코드}` 브랜치를 새로 만들어 작업한다.
- 커밋 메시지 형식: `[{단위코드}] {한 일 요약}`
- 브랜치는 base에 실제로 병합된 것을 확인(`git merge-base --is-ancestor`)한
  뒤에만 삭제한다 (`.github/pull_request_template.md` 체크리스트 참조).

## 참고 문서 경로

### 기본 문서 (00~06)
- **처음 읽는 5분 요약(전체 지도, 머메이드)**: `harness/harness_00_definition.md`
- 전체 SOP 및 타협 불가 규칙: `harness/harness_00_overview.md`
- TRD 템플릿: `harness/harness_01_trd_template.md`
- 작업지시서 템플릿: `harness/harness_02_work_order_template.md`
- 인수인계(A0) 템플릿: `harness/harness_03_handoff_template.md`
- 프롬프트 생성 규칙: `harness/harness_04_prompt_generator_template.md`
- 실행 인프라(브랜치/CI/권한/에스컬레이션): `harness/harness_05_execution_infra.md`
- 회고 및 실패 패턴 라이브러리: `harness/harness_06_meta_improvement.md`

### 확장 문서 (v1.1 — 2026-09-04 갭 분석으로 추가)
- 🔴 릴리스/우선순위 계획: `harness/harness_07_release_planning.md`
- 🔴 아키텍처 결정 기록(ADR): `harness/harness_08_adr.md`
- 🔴 배포/롤백 절차: `harness/harness_09_deployment_runbook.md`
- 🔴 개인정보 생애주기 정책: `harness/harness_10_data_lifecycle.md`
- 🔴 백업/재해복구 정책: `harness/harness_20_backup_dr.md` (2026-09-07 추가)
- 🟡 역할과 책임(RACI): `harness/harness_11_raci.md`
- 🟡 변경관리 프로세스: `harness/harness_12_change_management.md`
- 🟡 전역 기술 컨벤션: `harness/harness_13_tech_conventions.md`
- 🟡 테스트 데이터/시드 관리: `harness/harness_14_test_data_management.md`
- 🟡 병렬 작업/인터페이스 계약 관리: `harness/harness_15_parallel_work.md`
- 🟢 기술 부채 로그: `harness/harness_16_tech_debt_log.md`
- 🟢 모니터링/알림 기준: `harness/harness_17_monitoring.md`
- 🟢 AI 세션 비용/시간 추적: `harness/harness_18_cost_tracking.md`
- 🟢 템플릿 검증 체크: `harness/harness_19_template_validation.md`

## 확장 문서(07~20)는 왜 있어야 하는가 — 20년차 기획/설계/개발 관점

> 기본 문서(00~06)는 "AI에게 무엇을 어디까지 시킬지"를 다룹니다. 하지만 실제
> 프로젝트는 그 앞(기획)과 뒤(운영)가 반드시 붙습니다. 아래는 각 문서가 없을 때
> 실제로 터지는 문제를 근거로 설명한 것입니다 — "있으면 좋다"가 아니라
> "없으면 이렇게 됩니다"로 읽어주세요.

### 🔴 Critical (없으면 실제 프로젝트에서 반드시 사고가 남)

**`harness_07_release_planning.md` — 릴리스/우선순위 계획**
기존 SOP(`harness_00`)는 "위험도" 하나만으로 실행 순서를 정합니다. 그런데
위험도와 비즈니스 가치는 서로 다른 축입니다. 위험도만 보고 순서를 짜면 정작
급하게 써야 할 핵심 기능보다 위험한 부수 기능이 먼저 나오는
역전이 생깁니다. 20년차 PM이라면 반드시 MoSCoW/MVP 범위부터 확정하고 그
위에 위험도를 얹습니다 — 순서가 바뀌면 "다 만들었는데 정작 써야 할 건 마지막"
이라는 전형적인 실패가 재현됩니다.

**`harness_08_adr.md` — 아키텍처 결정 기록**
`harness_00 §4-1`은 "되돌리기 어려운 결정은 AI가 혼자 확정하지 못한다"고
못박아 놓고도, 정작 그 결정을 **어디에 기록하는지**는 정의하지 않았습니다.
기록이 없으면 3개월 뒤 "이 테이블을 왜 이렇게 정규화했지?", "왜 세션 방식이
아니라 토큰 방식을 골랐지?"에 아무도 답을 못하고, 새로 온 사람(혹은 새 AI
세션)이 같은 논의를 반복하거나 실수로 이미 폐기된 대안으로 되돌아갑니다.
설계자에게 ADR은 "코드 리뷰만큼 중요한 결정 리뷰"입니다.

**`harness_09_deployment_runbook.md` — 배포/롤백 절차**
`harness_05`는 정확히 "merge까지"만 다루고 끝납니다. merge된 코드가 실제
사용자에게 닿는 경로, 그리고 문제가 터졌을 때 되돌리는 절차가 없으면 두 가지
사고가 동시에 날 수 있습니다: (1) 코드는 완성됐는데 아무도 배포를 못 해서
방치되거나, (2) 급하게 즉흥적으로 배포하다가 장애가 나도 롤백 기준이 없어서
사태를 키웁니다. 20년차 엔지니어에게 "머지 = 완료"가 아니라 "머지는 배포의
시작"입니다.

**`harness_10_data_lifecycle.md` — 개인정보 생애주기 정책**
CLAUDE.md 원칙 7번("개인정보 보호")과 `harness_05 §3`은 **개발 단계**의
마스킹만 다룹니다. 정작 실제 운영 데이터베이스에 쌓이는 이용자/고객 개인정보를
언제까지 보관하고, 탈퇴/계약종료 시 언제 파기하며, 당사자가 열람·정정을 요청하면
어떻게 처리하는지는 완전히 비어 있었습니다. 개인정보는 법적 규제 대상이고,
이 공백은 기능 버그가 아니라 **법적 리스크**입니다. 그래서 이 문서는
`harness_00 §4-8`("되돌리기 어려운 결정 금지")의 적용 대상으로 명시했고, AI가
값을 채우지 못하도록 곳곳에 경고를 넣었습니다.

**`harness_20_backup_dr.md` — 백업/재해복구 정책**
`harness_10 §3` 파기 절차는 "관련 백업본 파기 일정 확인"이라며 백업의 *존재*를
전제로 쓰였지만, 정작 백업을 얼마나 자주 만들고 얼마나 보관하며 실제로
복구가 되는지 검증하는 절차는 어디에도 없었습니다. 백업은 만들어두는 것과
복구가 되는 것이 다른 문제입니다 — 복구 테스트를 한 번도 안 해본 백업은
장애 시점에야 손상됐다는 걸 알게 되는 경우가 흔합니다. 또한 코드 롤백
(`harness_09`)과 스키마 마이그레이션 롤백은 별개 문제라서, 이 문서가 없으면
"배포는 롤백했는데 DB는 계속 깨져 있다"는 사고가 반복됩니다. RPO/RTO 목표치는
`harness_00 §4-1`의 "되돌리기 어려운 결정" 적용 대상으로 명시했습니다.

### 🟡 Important (프로젝트/팀 규모가 커지면 반드시 아쉬워짐)

**`harness_11_raci.md` — 역할과 책임**
1인 프로젝트에서는 "누가 승인하나"가 안 보입니다 — 본인이 다 하니까요. 하지만
이 하네스를 다른 프로젝트, 다른 팀에 이식하는 순간 "TRD는 누가 최종 확정하고,
merge는 누가 승인하는가"가 불명확해지면 하네스 전체가 형식적인 서류로
전락합니다. 20년차 PM은 항상 "이 결정, 누구 책임이야?"를 먼저 묻습니다 —
이게 없으면 책임소재가 애매해질 때마다 프로세스가 아니라 사람 목소리 큰 쪽이
이깁니다.

**`harness_12_change_management.md` — 변경관리 프로세스**
`harness_00 §4-7`은 "스펙 문서는 단일 진실 공급원을 유지한다"고 선언만
합니다. 확정된 TRD를 실제로 바꿔야 할 때(요구사항 변경은 반드시 옵니다)
누가 승인하고, 이미 완료된 단위에 미치는 영향을 어떻게 분석하는지가 없으면,
결국 채팅으로 "이거 이렇게 바꿔주세요"가 스펙이 되고 문서는 낡습니다.
설계자 관점에서 이건 "스펙 드리프트"의 가장 흔한 원인입니다.

**`harness_13_tech_conventions.md` — 전역 기술 컨벤션**
각 단위 TRD의 §2(API 명세)는 단위별로 따로 정의됩니다. 전역 표준이 없으면
AI 세션마다, 사람마다 에러 응답 포맷/네이밍/인증 방식이 미묘하게 달라지고,
나중에 프론트와 백엔드를 조립하는 시점에 "부품이 안 맞는" 사고가 납니다.
20년차 개발자는 이걸 프로젝트 착수 시점에 딱 한 번 고정하고 그 뒤로는
협상하지 않는 것을 원칙으로 삼습니다.

**`harness_14_test_data_management.md` — 테스트 데이터/시드 관리**
`harness_05 §3`은 "마스킹 시드 데이터를 준비하라"고만 하고 그 시드를 누가
만들고 어디 있고 어떻게 재사용하는지는 정의하지 않았습니다. 이게 없으면
단위마다 계정을 새로 만들어 로그인 정보가 꼬이거나, 같은 테스트 대상인데
단위별로 다른 값을 써서 통합 테스트에서 앞뒤가 안 맞는 상황이 반드시 생깁니다.

**`harness_15_parallel_work.md` — 병렬 작업/인터페이스 계약 관리**
현재 SOP는 단위를 하나씩 순차로 진행하는 것을 전제로 합니다. 프로젝트가
커지면 서로 의존관계 없는 단위를 동시에 여러 세션으로 돌리고 싶어지는데,
인터페이스가 문서로 고정돼 있지 않으면 한쪽이 API를 바꿔도 다른 쪽은
그걸 모른 채 진행하다가 통합 시점에 깨집니다. 설계자에게 이건 고전적인
"팀 간 계약(contract) 관리" 문제이고, AI 멀티세션 환경에서는 더 빨리 터집니다.

### 🟢 Nice-to-have (있으면 하네스가 더 단단해짐)

**`harness_16_tech_debt_log.md` — 기술 부채 로그**
A0 인수인계 §3(편차)는 "몰랐는데 다르게 나온 것"을 기록하는 곳이라, "알면서
의도적으로 미룬 것"을 여기에 섞으면 문서가 지저분해지고 미룬 일들이 시간이
지나면 잊힙니다. 20년차 개발자는 기술부채를 "감정이 아니라 목록"으로
관리해야 나중에 실제로 갚는다는 걸 압니다.

**`harness_17_monitoring.md` — 모니터링/알림 기준**
`harness_05 §5`의 관찰가능성은 개발 단계 로그(에이전트가 뭘 했는지)만
다룹니다. 운영 중 시스템이 실제로 죽었는지, 느려졌는지, 개인정보에 이상
접근이 있었는지는 완전히 다른 문제이고, 이게 없으면 사용자 신고가 유일한
장애 감지 수단이 됩니다.

**`harness_18_cost_tracking.md` — AI 세션 비용/시간 추적**
`harness_06` 회고는 "시간을 절약했는가"를 정성적으로만 묻습니다. 하네스가
실제로 효율적인지, 어느 단계에서 재시도가 몰려 비용이 새는지는 숫자 없이는
판단이 안 되고, 숫자가 없으면 하네스 개선도 감으로만 하게 됩니다.

**`harness_19_template_validation.md` — 템플릿 검증 체크**
지금 모든 문서는 사람이 체크리스트를 눈으로 보고 스스로 확인하는 방식입니다.
바쁘면 빈칸을 건너뛰고 다음 단계로 넘어가기 쉽습니다. 이 문서는 각 문서
유형별 필수 필드를 명시해서, 나중에 간단한 스크립트로도 기계 검증할 수
있는 형태로 정리해 둔 것입니다.

## 알려진 실패 패턴 (harness_06 §3 연동 — 새로 발견되면 여기와 그 파일에 함께 추가)

이 저장소 자체는 도메인 중립을 유지해야 하므로, 특정 프로젝트에서 실제로
겪은 사고의 상세 서술(날짜/프로젝트명/구체적 파일명 등)은 여기 남기지 않고
`harness/harness_06_meta_improvement.md` §3 "실패 패턴 라이브러리"에 **일반화된
형태**(구체 사례를 제거하고 재사용 가능한 교훈만)로만 누적합니다. 새 패턴을
발견하면 반드시 그 형태(패턴명/설명/재발방지책, 프로젝트 식별 정보 없이)로
그 표에 추가하세요.

## 작업 이력 (Work Log)

> 사용자 지시(2026-09-07): 이 프로젝트에서 진행한 주요 작업 이력을 세션이
> 바뀌어도 이어볼 수 있도록 여기에 시간순으로 남깁니다. 컨펌 없이 AI가
> 자동으로 결정하고 진행한 항목의 상세 근거는 `자동진행/` 폴더의 개별
> 로그 파일을 참조하세요(여기서는 요약만).

### 2026-09-07
- 사용자가 `00 파이널 프로젝트 계획서/` PDF 2건("웹 AI 모의면접 플랫폼")을
  기준으로 파이널 프로젝트 진행을 지시. 이 저장소가 원래 도메인 중립
  하네스 골격(HARNESS_BASIC)이라는 CLAUDE.md 원안과 충돌함을 확인 →
  AskUserQuestion으로 확인 → **이 저장소를 프로젝트 저장소로 전환**하기로
  사용자 승인 (CLAUDE.md/README.md "프로젝트 개요"/"저장소 전환 결정"
  섹션에 반영).
- PDF 2건 검토 결과, 원본 기획이 엔터프라이즈 프로덕션 스펙(Kubernetes,
  Oracle, Pinecone, Hume AI, GCP, 팀 7명·7주)임을 확인. AskUserQuestion으로
  실제 제약 확인: **1인 개발, 4주 이내, Claude Code 구독료 외 전부 무료**.
  이어서 실시간 스트리밍 대화(REQ-N-001) 구현 가능성을 재확인 →
  **턴 기반(녹음 버튼) 방식**으로 확정.
- 위 결정에 따라 ADR 5건 작성(`docs/adr/adr-001`~`adr-005`): 인터뷰
  상호작용 모드, AI 파이프라인 스택(Gemini/faster-whisper/브라우저 TTS/
  DeepFace+librosa), 데이터 스택(PostgreSQL+pgvector, Redis/Celery 제거),
  미디어 캡처 범위(WebRTC SFU 제거), 배포(로컬 Docker Compose).
- 마스터 TRD(`docs/trd/aimock_master_trd.md`), 릴리스 계획(`docs/release/
  release_plan.md`, MoSCoW+위험도×가치 매트릭스+4주 마일스톤), 전역 기술
  컨벤션(`docs/tech_conventions.md`) 작성 완료 — harness Phase A 1~5단계
  완료.
- 자동진행 로그 1건 작성(`자동진행/MVP아키텍처축소결정_20260907195304.md`):
  사용자가 승인한 상위 방향(무료/1인/4주/턴기반) 안에서 구체적 라이브러리
  선택과 하네스 적용 범위 축소(RACI/변경관리 문서 생략)를 AI가 자동 결정한
  근거 기록.
- **다음 단계(사람 확인 대기)**: (1) 원본 오디오/비디오 영구 저장 여부
  (ADR-004 미결), (2) 계정 삭제/탈퇴 시 데이터 처리 정책(원칙 8 대상) —
  이 두 가지 확정 후 Phase B(단위별 TRD+작업지시서+실제 코드 착수) 시작.
- 위 두 가지를 AskUserQuestion으로 다시 확인하려 했으나, 질문 UI가 긴
  한글 문장을 음절 단위로 깨뜨려 표시하는 버그를 일으켜 사용자가 스크린샷
  (`화면캡쳐/진행상황_01.png`)으로 지적. 이후 중요한 결정 질문은 UI 대신
  채팅 평문으로 전환. 버그는 SendFeedback으로 별도 기록.
- 사용자가 "하네스 시스템을 실제로 기반으로 하고 있는지" 재확인을 요구
  (신뢰 문제 제기, 실수 재발 금지 강조) → `harness_00_overview.md`(원문
  SOP), `harness_02`(작업지시서), `harness_03`(A0), `harness_05`(실행
  인프라), `harness_10`(개인정보 생애주기)을 처음으로 전문 정독하고
  기존 산출물을 전부 재검증. 발견한 실제 갭과 조치:
  - `harness_00_overview.md` §5 파일명 규칙(영문/숫자, `{프로젝트코드}_
    {단위코드}_{문서유형}.md`)을 몰랐던 채로 `docs/trd/00_master_trd.md`를
    만들었던 것을 발견 → 프로젝트 코드 `aimock` 확정, 파일을
    `aimock_master_trd.md`로 리네임하고 모든 참조 갱신, 규칙을
    `docs/tech_conventions.md`에 명문화.
  - `harness_05_execution_infra.md`가 요구하는 "CLAUDE.md에 기준 브랜치
    명시 + CI의 대상 브랜치와 일치"가 안 되어 있었음(`ci.yml`은 `main`
    기준, 실제 저장소 기준 브랜치는 `PROD_SCH`) → CLAUDE.md에 기준 브랜치
    `PROD_SCH` 명시, `ci.yml` 수정.
  - `harness_05` §2가 이미 존재한다고 전제한 `.github/workflows/ci.yml`의
    `dependency-scan`/`lint`/`test` 잡이 "스택 확정 후 채우기" placeholder
    상태였음 → 확정된 스택(Python/FastAPI, TS/React, pgvector) 기준으로
    실제 내용(pip-audit/npm audit, ruff+mypy/eslint, pytest+postgres 서비스
    컨테이너)을 채움.
  - 마스터 TRD §2/§3에 "[사람 작성]"/"[사람 확인 완료]"로 표시돼 있던
    부분이 실제로는 AI 초안 + 상위 방향 승인만 있던 것이라 라벨을
    "[AI 초안 → 사람 확인 필요]"로 정정하고, A0 인수인계 문서
    (`docs/handoff/aimock_a0_handoff.md`)를 신규 작성해 이 갭들과 §4 남은
    작업으로 명시.
  - 결론: Phase A 절차(harness_00_definition §4) 자체는 따랐으나, 원문
    SOP(harness_00_overview)를 사전에 전부 읽지 않아 파일명·CI·브랜치
    규칙을 놓친 것이 이번에 드러난 실제 실수. 재발 방지책 반영 완료 —
    `harness_06_meta_improvement.md` §3에 "Phase A 착수 시 요약본만 읽고
    원문 SOP·실패패턴 라이브러리를 건너뜀" 패턴 추가, `harness_00_definition.md`
    상단에 원문 선독 경고 추가.
- 재검증 완료 후 남아있던 미결 정책 2건을 사용자에게 평문으로 재확인 →
  **(1) 원본 오디오/영상: 지원자가 직접 삭제 요청 전까지 로컬 암호화
  보관**(ADR-004 갱신), **(2) 계정 탈퇴: 소프트삭제 + 30일 유예기간 후
  스케줄러 자동 물리삭제**(ADR-006 신규 작성)로 확정. 반영 내역: ADR-003
  ERD에 `MEDIA_ASSETS`, `USERS.deleted_at/purge_at` 추가 / 마스터 TRD
  §3(N-003 재정의, N-005 신규)·§5(AC-M6/M7 추가)·§7(미결 2건 해소) 갱신 /
  릴리스 계획에 `U1-b`(미디어 저장+삭제) 단위 신규 추가, Week1 범위에
  탈퇴 스케줄러·미디어 삭제 API 포함 / A0 인수인계 §6 갱신.
- **Phase A 완전 종료.** 다음 단계: U1(인증/계정+탈퇴)과 U1-b(미디어
  저장+삭제) 단위 TRD 작성부터 Phase B 시작.
- **Phase B 착수 — U1+U1-b 구현 및 실행 검증 완료(같은 날 계속)**:
  단위 TRD 2건(`docs/trd/aimock_u1_trd.md`, `aimock_u1b_trd.md`)과
  작업지시서(`docs/workorder/aimock_u1_workorder.md`) 작성 후 실제 코드
  작성. **"코드가 돈다"를 말로만 하지 않고 실제로 확인**했다: Docker로
  `pgvector/pgvector:pg16` 컨테이너를 띄우고, Alembic으로 ADR-003 ERD 전체
  (8개 테이블 + ADR-006 컬럼)를 실제 마이그레이션해 적용, U1/U1-b의 AC
  12개(7+5) 전부를 mock 없이 실제 DB에 대해 pytest로 실행해 전부 pass
  확인, `ruff`/`mypy` 클린, Docker 이미지 빌드 후 컨테이너로 띄워
  signup→login→me→withdraw→me(401) 흐름을 curl로 직접 호출해 재확인.
  과정에서 실제로 발견/수정한 문제 4건(passlib-bcrypt 호환성, Docker
  이미지의 email-validator 누락, 로컬 5432 포트 충돌, pytest-asyncio
  이벤트루프 불일치)은 A0 인수인계 §3에 기록.
- git 커밋/브랜치/PR은 CLAUDE.md 최우선 규칙(Claude는 git 작업 안 함)에
  따라 하지 않음 — 파일은 워킹트리에만 존재, 사용자가 리뷰 후 직접
  커밋해야 함. 다음 단계는 U2-a(턴 기반 질문-답변) 단위 TRD 작성.
- **사용자 지시로 "내부 테스트 결과 및 절차 문서화 규칙" 신설**(이 문서
  상단 참조): 내부적으로 테스트 가능한 모든 기능은 실제로 테스트하고
  결과를 `내부테스트결과서/{작전명}_{yyyyMMddHHmmss}.md`로 남길 것, 절차적
  내용은 항상 mermaid로 작성할 것. 사용자가 미리 만들어둔
  `내부테스트결과서/` 폴더 이름을 그대로 채택(제가 처음에 문장에서 다르게
  유추해 "내부테스트 결과"라는 별도 폴더를 만들었다가, 사용자가 이미
  만들어둔 폴더가 있음을 확인하고 그 폴더 하나로 통일함). 이 규칙을 즉시
  적용해 U1/U1-b 테스트 결과를 소급 작성
  (`내부테스트결과서/U1_U1b_인증탈퇴미디어저장삭제테스트_20260907205930.md`).
- **Phase B — U2-a(턴 기반 질문-답변) + U3-a(STT/LLM 파이프라인) 구현 및
  검증 완료(같은 날 계속)**: 단위 TRD 2건(`aimock_u2a_trd.md`,
  `aimock_u3a_trd.md`)과 작업지시서(`aimock_u2a_workorder.md`) 작성 후
  구현. `LLMProvider`(Gemini, `google-genai` SDK 실제 설치·시그니처
  확인 후 구현)와 `STTProvider`(faster-whisper) 어댑터 패턴 + 테스트용
  Fake 구현(`app.dependency_overrides`로 교체). 면접 시작/턴 제출/종료
  API, 질문은행 시드 10문항. **faster-whisper는 실제로 tiny 모델을
  다운로드해 실제 오디오(WAV)로 동작을 수동 검증**(의미있는 인식
  정확도는 실제 음성 필요라 범위 밖). Gemini는 `GEMINI_API_KEY`가 없어
  실제 호출은 못 했음 — 다음에 키를 넣으면 그 부분만 재검증 필요.
  pytest 21/21 pass(U1+U1-b+U2-a+U3-a 누적), ruff/mypy 클린. 구현 중
  `transcripts.created_at`을 DB `server_default now()`에서 Python
  기본값으로 바꿔야 하는 버그(한 트랜잭션 내 여러 INSERT가 전부 같은
  타임스탬프를 받아 대화 순서가 뒤섞임)를 발견해 수정 — A0 §3에 기록.
  내부테스트결과서에도 별도 기록
  (`U2a_U3a_턴기반질문답변STT_LLM파이프라인테스트_20260907211637.md`).
- **사용자 지시("내부테스트 안한 건 테스트해라")로 미검증 항목 소급 테스트
  (같은 날 계속)**: 이전에 "이번엔 안 함"이라고 남겨둔 두 가지를 실행 —
  (1) Docker `app` 이미지를 새 의존성(`google-genai`, `faster-whisper`)
  포함해 재빌드하고 실제 HTTP로 signup→interview시작→media업로드/삭제→
  turn제출→end 전체 흐름 호출, (2) Alembic `downgrade -1`/`upgrade head`
  라운드트립. **실제 버그 2건을 이 과정에서 발견해 고쳤다**:
  `requirements.txt`의 `pydantic==2.10.4`가 `google-genai`의 요구
  버전(`>=2.12.5`)과 충돌해 Docker 빌드 자체가 실패(→ `2.13.4`로 상향),
  `GEMINI_API_KEY` 없이 턴 제출 시 500이 나던 것을 503(`ServiceUnavailableError`)
  으로 정리(TRD에 AC-8로 추가, 자동화 테스트 추가로 pytest 22/22).
  faster-whisper가 컨테이너 안에서 실제로 로드/실행됨을 로그(HF Hub 경고)
  로 확인. Alembic 롤백도 정상 동작 확인(테이블 10개 삭제→복구).
  결과 기록: `내부테스트결과서/Docker전체스택실HTTP및마이그레이션롤백테스트_20260907213116.md`.
- **사용자가 "이번 세션은 컨펌 없이 자동으로 다음 작업까지 진행"을
  명시적으로 승인**(조건: 20년차 기준 유지, 자동진행/내부테스트결과서
  반드시 남길 것) → **U2-b(라이브 코딩) 구현 완료(같은 날 계속)**:
  `docs/adr/adr-007-code-execution-sandbox.md`(신규, ⚠️ subprocess는
  진짜 샌드박스가 아니라는 보안 한계를 명시적으로 문서화),
  `aimock_u2b_trd.md`, `aimock_u2b_workorder.md` 작성 후 `CodeExecutor`
  어댑터+`SubprocessExecutor`(5초 타임아웃, 환경변수 allowlist로 앱
  시크릿 차단) 구현. 자동진행으로 결정한 것 2가지(보안 아키텍처를 사람
  승인 없이 확정 / AI 코드 평가를 U4로 이동) 근거는
  `자동진행/U2b샌드박스및작업범위자동결정_20260907213841.md`에 기록.
  실제 subprocess로 pytest 7건 실행(전체 29/29 pass) + 실제 Docker
  컨테이너에 curl로 정상코드/무한루프(5.08초 실측)/시크릿유출시도
  3가지를 직접 호출해 확인 — 앱 시크릿(DB URL/JWT/암호화키/LLM 키)이
  전혀 노출 안 됨을 실제로 검증. 결과 기록:
  `내부테스트결과서/U2b_라이브코딩샌드박스테스트_20260907214804.md`.
- **다음 단위는 U4(피드백 리포트)로 AI가 자동 선정** — U2-c(화이트보드)
  는 Vision 프롬프트 튜닝+캔버스 UI가 필요해 위험 대비 가치가 낮고,
  U3-b(표정/음성분석)는 실제 얼굴 이미지 샘플이 없어 지금은 의미있게
  검증할 수 없음. U4는 이미 저장된 데이터(transcripts,
  coding_submissions)만으로 구현·검증 가능해 다음으로 적합하다고
  판단(release_plan.md도 이 순서로 갱신). 사용자 승인에 따라 되묻지
  않고 계속 진행.
- **U4(피드백 리포트) 구현 완료(같은 날 계속, 자동진행)**:
  `aimock_u4_trd.md`, `aimock_u4_workorder.md` 작성 후 `ReportGenerator`
  어댑터(U2-a/U3-a와 동일 패턴) + LLM 불필요한 로컬 키워드 추출기
  구현. 표정/음성 타임라인은 U3-b 미착수라 빈 배열로 처리(자동진행
  판단). pytest 8건 신규(전체 37/37 pass), ruff/mypy(50개 파일) 클린,
  실제 Docker 컨테이너에서 live상태생성시도(409)/키없이생성(503)/
  미생성조회(404) curl로 직접 확인. 결과 기록:
  `내부테스트결과서/U4_피드백리포트생성조회테스트_20260907215737.md`.
  다음 단위는 U5(대시보드) — U2-c/U3-b는 여전히 선행조건(Vision
  프롬프트 튜닝, 실제 얼굴 이미지) 미충족으로 보류.
- **U5(채용담당자 대시보드) 구현 완료(같은 날 계속) — Must-have 전체
  완료**: `aimock_u5_trd.md`, `aimock_u5_workorder.md` 작성 후
  `require_recruiter` 의존성 + 목록/리포트열람/통계 API 구현. pytest
  6건 신규(전체 **43/43 pass**), ruff 클린, mypy 53개 파일 이슈 없음,
  실제 Docker 컨테이너에서 candidate(403)/recruiter(200) 권한 분리를
  curl로 직접 확인. 결과 기록:
  `내부테스트결과서/U5_채용담당자대시보드테스트_20260907220531.md`.
  **🎯 마스터 TRD Must-have 단위 전체(U1/U1-b/U2-a/U3-a/U2-b/U4/U5)
  구현·검증 완료.** 이제부터는 사람이 결정해야 하는 지점(GEMINI_API_KEY
  발급, 프론트엔드 착수 여부, U3-b용 웹캠 샘플 제공 여부, U2-c 시도
  여부, 전체 diff 리뷰+git 커밋)만 남음 — A0 §4에 정리.
- **프론트엔드(React/Vite) 전체 화면 구현 완료(2026-09-07, 신규 세션 —
  사용자가 "긴급/본인 처리 아니면 자동진행" 명시적으로 재승인 후 인계받아
  계속)**: `docs/trd/aimock_frontend_trd.md`(화면 5개 통합 TRD, 판단
  근거는 `자동진행/프론트엔드TRD통합및착수결정_20260907232301.md`),
  `docs/workorder/aimock_frontend_workorder.md` 작성 후
  `src/frontend/`(Vite+React+TypeScript) 전체 구현: 로그인/회원가입,
  면접장(턴 기반 질문-답변 + MediaRecorder 녹음 + 오디오 파일 업로드
  폴백), 라이브 코딩(Monaco 에디터), 피드백 리포트, 채용담당자 대시보드.
  백엔드 `main.py`에 CORS 미들웨어 추가(프론트 dev 서버 크로스오리진
  허용 — 새 기능 아닌 최소 배선).
- **AC 대비 결과(AC-F1~F10, 실제 Docker 백엔드 + Vite dev 서버 + Claude
  Browser 도구로 실제 브라우저 조작, mock 없음)**: **10개 중 9개 PASS**,
  AC-F6(면접 턴 제출→다음 질문 갱신)은 `GEMINI_API_KEY` 미확보로 부분
  검증(계약은 curl로 실증, LLM 응답 갱신 자체는 키 확보 후 재검증 필요).
  상세는
  `내부테스트결과서/프론트엔드_U1U2aU2bU4U5화면수동테스트_20260907233851.md`.
- **테스트 중 실제 버그 발견 및 수정**: `ReportPage.tsx`가 리포트 생성
  409 응답을 "이미 생성 중"으로 잘못 가정해 하드코딩 문구를 보여주고
  있었음 — 실제로는 `report_service.generate_report()`의 유일한 409
  조건이 "면접이 아직 완료 안 됨"이었음(원본 코드 재확인 후 발견,
  하네스 원칙 6). 서버가 보내는 실제 메시지를 그대로 노출하도록 수정하고
  재검증 완료.
- **프로덕션 강화 8건 완료(2026-09-08, 사용자의 "20년차 재검토" 요청 →
  8개 항목 전부 승인받아 자동진행)**: 프론트 유닛테스트(Vitest 16건),
  CI 프론트 빌드/테스트 단계, nginx 프로덕션 정적 서빙(`docker compose
  --profile prod`), 로그인 브루트포스 방어(레이트리밋, 인메모리
  슬라이딩 윈도우), JWT 만료 시 자동 로그아웃, ADR-007 샌드박스
  `resource.setrlimit` 추가(CPU/메모리 상한, 400MB 메모리폭탄 코드로
  실제 컨테이너에서 차단 확인), dompurify 취약점 패치(`npm overrides`,
  0 vulnerabilities), 마스터 TRD §2/§3 라인 검토. 판단 근거:
  `자동진행/20260908_프로덕션강화8건_자동진행결정.md`. 검증 결과:
  `내부테스트결과서/20260908_프로덕션강화8건_검증결과.md`.
- **라인 검토 중 발견해 즉시 수정한 실제 버그**: 면접 턴 제출이
  STT에만 오디오를 쓰고, 이미 승인된 ADR-004(원본 오디오 암호화 보관)가
  요구하는 저장을 호출하지 않고 있었음 → `interview_service.submit_turn()`
  에 연결. AC-M6(리포트 화면에서 원본 미디어 삭제)도 API만 있고 프론트
  UI가 없었음 → `GET /api/v1/interviews/{id}/media`(신규) +
  `ReportPage.tsx` "원본 답변 오디오 관리" 섹션 구현. 둘 다 실제 Docker
  컨테이너+브라우저로 재검증, pytest 4건 추가(**전체 47/47 pass**).
  마스터 TRD 라인 검토 상세 결과(문서 문구가 실제 구현보다 과장된 부분
  등)는 `docs/trd/aimock_master_trd_review_20260908.md` — 사람 승인
  대기 중.
- **(같은 날 이후 진행분 — 상세 로그는 `docs/handoff/aimock_a0_handoff.md`
  §2 참조, 여기는 요약만)** Gemini 무료 티어 일일 쿼터 소진 발견 →
  Groq로 전환(재전환 가능한 어댑터 구조 유지), 전환 검증 중 "동일 질문
  반복" 버그가 LLM 제공자 문제가 아니라 `question_service`의 질문 후보
  중복 제거 누락이었음을 실측으로 확인해 수정, 로컬 `pytest`가 실제
  개발 DB를 매번 TRUNCATE하던 심각한 데이터 유실 버그 발견·수정
  (`tests/backend/conftest.py`), ADR-007 1단계 보안 강화(seccomp+비root
  권한하락) 완료·실제 exploit 스크립트로 검증, U3-b(DeepFace+librosa)
  엔진과 실시간 웹캠 캡처 UI 구현.
- **리포트 화면 검은 화면 캡처 버그 발견·수정 + 화면 재설계 + TRD
  동기화(2026-09-08)**: 사용자가 공유한 실제 리포트 화면에서 표정
  타임라인이 전부 "unknown"인 것을 실제 DB 미디어를 복호화해 확인한
  결과, 캡처된 프레임이 전부 검은 이미지였음을 발견 — 원인은
  `InterviewPage.tsx`가 숨긴(`hidden`) `<canvas>`를 재사용하던 것,
  인메모리 캔버스로 교체(실카메라 재검증은 사용자 확인 대기 중).
  리포트 상세가 원본 JSON을 그대로 노출하던 것도 발견해
  `ReportDetails.tsx`로 재설계. 이 과정에서 마스터 TRD F-006이 "음성
  운율" 항목을 누락하고 있었고 프론트엔드 TRD엔 리포트 화면 레이아웃
  스펙 자체가 없었던 문서 공백을 발견 → `aimock_master_trd.md`/
  `aimock_frontend_trd.md`(§1-1, AC-F11 신설) 동기화. 상세:
  `내부테스트결과서/리포트화면TRD정합성점검및문서동기화_20260908124216.md`.
- **전체 화면 TRD 실측 재검증(AC-F1~F11 전부 PASS) + 실제 버그 발견·수정
  (2026-09-08)**: 사용자 문의("질문이 14개 고정인가", "질문이 백엔드만
  나오는가")에 코드+DB 실측으로 답변(둘 다 버그 아님 — LLM이 job_role
  텍스트를 따라 스스로 판단하는 설계상 정상 동작, job_role을 바꾸면
  질문 주제도 실제로 바뀜을 직접 실측). 이 과정에서 "원본 답변 오디오
  관리" 화면의 시각 표시가 이상한 것을 포착 → `users`/`media_assets`
  등 5개 테이블의 `created_at` DEFAULT가 `now()` 함수가 아니라 **최초
  마이그레이션 적용 시각으로 고정된 리터럴**이었던 실제 버그를 발견·
  수정(모델 5개 파일 + 신규 마이그레이션 `f6341f660dd1`). pytest
  52/52 회귀 없음. 상세:
  `내부테스트결과서/전체TRD화면실측및질문개수직무필터점검_20260908132406.md`.
- **Render 배포 지원 + 운영 환경에서 실사용자가 보고한 실제 결함 2건
  수정(2026-09-08, 같은 날 계속)**: 사용자가 Render(웹서비스)+Neon(관리형
  Postgres)으로 처음 배포를 진행하는 전 과정(Dockerfile Path/Build
  Context, `$PORT` 바인딩, Neon 연결 문자열 변환, pgvector 확장,
  Persistent Disk, Health Check Path, Pre-Deploy Command로 Alembic 자동
  마이그레이션 등)을 스크린샷 기반으로 단계별 안내해 실제 배포 완료.
  배포 후 사용자가 실사용 중 보고한 문제 2건을 실제로 조사·수정:
  1. **턴 제출 응답 10초 이상**: 원인은 `faster-whisper`(로컬 STT)가
     Render 저사양 인스턴스(0.5 vCPU)에서 CPU 바운드로 느린 것 —
     로컬 개발 PC에서는 안 보이던 문제. 사용자 승인(ADR-008 신규,
     하네스 원칙 8 대응)을 받아 STT를 **Groq 호스팅 Whisper API**로
     전환. 로컬에서 실제 GROQ_API_KEY로 검증: webm/opus 샘플 기준
     응답 0.48초, 전체 턴 처리(암호화저장+STT+RAG+LLM) 1.84초로
     대폭 개선(Render 운영 환경 재실측은 사용자가 배포 후 확인 필요).
  2. **면접이 턴 25까지 진행돼도 안 끝남**: 원인은 LLM 프롬프트에
     목표 질문 수 지시가 없고 서버에도 상한이 없던 설계 공백. 사용자
     확정값(최소 5개/최대 10개, 오프닝 포함)으로
     `interview_min_questions`/`interview_max_questions` 설정 도입 —
     프롬프트 안내(1차) + 서버 강제(`_enforce_question_count_bounds`,
     2차 안전장치) 이중 구조로 구현.
  두 수정 모두 pytest 신규 7건 포함 전체 59 passed(회귀 없음),
  ruff/mypy 클린, 실제 Docker 컨테이너 + 실제 Groq API 호출로 종단 간
  스모크 테스트 완료. `docs/adr/adr-008-stt-groq-hosted-whisper.md` 신규,
  `aimock_u2a_trd.md`(AC-9/AC-10 신규)/`aimock_u3a_trd.md`(AC-5 신규)/
  `aimock_master_trd.md`(N-001 갱신) 동기화. 상세:
  `내부테스트결과서/U2a보완_STT전환및질문개수제한_20260908172949.md`.
  **다음 액션(사람 필요)**: 이 변경분 git 커밋·푸시 → Render 재배포 →
  운영 환경에서 두 문제가 실제로 해소됐는지 최종 재확인.
- **피드백 리포트 생성 비동기화(2026-09-08, 같은 날 계속) — 사용자가
  운영 환경에서 "리포트 생성이 1분 이상 걸린다" 재보고, "20년차 기준으로
  절대 틀리면 안 됨" 명시적 강조 후 진행**: 원인은
  `report_service.generate_report()`가 HTTP 요청 하나 안에서 LLM 호출 +
  턴마다 저장된 `video_frame` 전부를 DeepFace로, `audio` 전부를
  librosa로 순차 동기 분석하던 것 — **마스터 TRD 원래 아키텍처 다이어그램
  에는 이미 "표정/음성운율 분석은 BackgroundTasks로 비동기 처리"라고
  명시돼 있었는데, 실제 U4 구현 시 이 부분이 누락돼 전부 동기로 짜여
  있었음**(새 요구사항이 아니라 원본 설계를 실제로 구현하는 것, 원본
  우선 원칙으로 발견). 코드를 짜기 전에 먼저 **"httpx `ASGITransport`가
  FastAPI `BackgroundTasks`를 언제 실행하는가"를 별도 실험으로 실측
  검증**(테스트 설계가 틀린 가정 위에 서지 않도록 — 결과: 응답이 조립된
  후, `client.post()`가 반환되기 전에 전부 실행됨을 확인) 후 설계 착수.
  `evaluation_reports`에 `status`/`error_message`/`processing_started_at`
  컬럼 추가(마이그레이션 `a1c3e7f2b904`, 기존 행은 `completed`로 백필),
  `report_service`를 `start_report_generation`(빠른 시작, 중복 실행
  방지 + IntegrityError 경합 복구 + 5분 좀비 작업 복구)과
  `run_report_generation`(백그라운드 전용, 요청 세션이 아닌 독립
  `AsyncSessionLocal` 사용 — FastAPI BackgroundTasks의 잘 알려진 함정
  회피)으로 분리. `POST /report`는 이제 항상 `202`+`processing`만 즉시
  반환(더 이상 503 없음 — AI 실패는 `status=failed`+안전한 일반 메시지로
  표현, 원본 예외 텍스트는 서버 로그에만 남기고 사용자에게 노출 안 함).
  프론트(`ReportPage.tsx`)는 2초 간격 폴링 도입, `RecruiterReportPage.tsx`
  도 상태 인지하도록 갱신. pytest 63 passed(신규 4건 포함, 회귀 없음),
  ruff/mypy 클린, 프론트 tsc/vitest(17개, 신규 2건)/oxlint 클린.
  **실제 Docker(uvicorn) 컨테이너로 종단 간 검증**: 실제 POST 응답
  0.155초(이전 1분+ 대비 개선 실측), 폴링 1초 후 실제 Groq 결과로
  completed 전환 확인. **`curl` 2개를 진짜 동시에 발사해 경합 상황을
  실제 서버로 재현** — (1) 이미 있는 리포트 재생성 동시 요청, (2) 첫
  생성 시 INSERT 유니크 제약 경합 유도 — 두 경우 다 DB에 정확히 1개
  행만 남고 에러/중복 없음을 실제로 확인(유닛테스트로 결정론적 재현이
  어려운 DB 레벨 경쟁 상태를 실제 서버 테스트로 보완). 상세:
  `내부테스트결과서/U4보완_리포트생성비동기화_20260908180942.md`.
  **다음 액션(사람 필요)**: 이 변경분도 git 커밋·푸시 → Render 재배포 →
  운영 환경에서 리포트 생성 화면이 더 이상 멈추지 않는지 최종 재확인.
- **리포트 화면 프론트 캐시 문제 진단 + 음성 운율 분석 속도 개선(2026-09-08,
  같은 날 계속)**: 위 비동기화 배포 직후 사용자가 "리포트 화면에 점수가
  `-`로만 나온다"를 재보고. git 커밋 로그(`025732f`)와 Render 두 서비스의
  Deploys 탭(백엔드/프론트 둘 다 확인 요청)을 대조해 **둘 다 최신 커밋으로
  정상 배포된 상태**임을 확인 → 브라우저 캐시 문제로 좁혀 강력
  새로고침(Ctrl+Shift+R) 안내 → 실제로 캐시 문제였음(사용자 확인).
  이어서 "리포트 생성이 여전히 1분 이상 걸린다"는 재보고에 **추측하지
  않고 로컬에서 각 연산을 직접 벤치마크** — DeepFace가 병목일 거라
  예상했으나 실측 결과 웜 추론 0.04초로 병목이 아니었고, **진짜 병목은
  `librosa.pyin`(피치 탐지, 확률적 Viterbi 디코딩)**: 8.75초 오디오
  기준 3.16~3.39초로 다른 연산 대비 압도적으로 느렸음. 오디오를 8kHz로
  다운샘플 + 피치 탐색 상한을 C7→C6로 축소(사람 음성 범위는 그대로 다
  덮음)해 **0.69초로 개선(약 4.7배), 피치 정확도 오차는 2% 수준**(둘 다
  로컬 실측 벤치마크로 확인, 알고리즘 자체는 그대로 유지해 정확도 손실
  최소화). 이 과정에서 재발방지용으로 단계별 소요시간 로그를 추가하다
  **더 심각한 부수 발견**: 이 프로젝트 전체가 로깅 레벨/핸들러를 한 번도
  명시적으로 설정한 적이 없어 `logger.info(...)` 호출이 전부 처음부터
  무음이었음(WARNING만 파이썬 내장 최후 폴백으로 우연히 보였던 것) —
  `app/main.py`에 `"aimock"` 로거 전용 핸들러를 붙여 수정, Docker
  컨테이너에서 실제로 로그가 찍히는 것까지 재검증 완료. pytest 64
  passed(신규 1건), ruff/mypy 클린. 일반화한 교훈 2건을
  `harness/harness_06_meta_improvement.md` §3에 신규 등록(저사양 클라우드
  ML 추론 벤치마크 필요성, 로깅 설정 누락 패턴). 상세:
  `내부테스트결과서/U3b보완_음성운율분석속도개선및로깅수정_20260908195613.md`.
  **다음 액션(사람 필요)**: git 커밋·푸시 → Render 재배포 → Logs 탭에서
  실제 단계별 소요시간을 확인(이제 로그가 보이므로 여전히 느리면 다음
  최적화 대상이 로그로 바로 드러남) → 그래도 느리면 Compute 플랜 상향
  또는 분석 대상 축소(턴 표본화) 중 택일이 필요하며 이는 비용/기능
  트레이드오프라 사람 결정 사항.