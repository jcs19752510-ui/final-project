# 커밋 전 시크릿 유출 방지 (harness_05 §3 "위험 명령 차단" 원칙을 기술적으로 강제)
# 설치: pip install pre-commit && pre-commit install
# 이 파일은 스택과 무관하게 그대로 사용 가능 (모든 파일을 스캔)
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
