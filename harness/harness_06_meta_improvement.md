# 개선(메타) 계층 (Layer 3 — 하네스 자체를 진화시키기)

> 지금 만든 5+1개 문서(00~05)는 "현재 시점의 최선"일 뿐, 정답이 아닙니다.
> 실제로 써보면 안 쓰이는 필드, 빠진 필드가 드러납니다. 이 계층은
> 그 학습을 놓치지 않고 하네스 자체를 버전업하는 절차입니다.

---

## 1. 하네스 버전 관리

- 하네스 문서 자체도 `harness/` 폴더에서 버전관리(Git)한다.
- 프로젝트 하나가 끝날 때마다 하네스 버전을 올린다 (v1.0 → v1.1 등).
- 변경 이력을 아래 표로 남긴다.

| 버전 | 날짜 | 변경 내용 | 계기가 된 프로젝트/사건 |
|---|---|---|---|
| v1.0 | | 최초 작성 | |
| v1.1 | 2026-09-04 | `harness_07`~`harness_19` 13종 추가 (릴리스계획/ADR/배포롤백/개인정보생애주기/RACI/변경관리/기술컨벤션/테스트데이터/병렬작업/기술부채/모니터링/비용추적/템플릿검증) + `harness_00_definition.md`(전체 지도) 추가 | 실제 프로젝트 적용 중 "20년차 기획/설계/개발자 기준 갭 분석" 요청 — 기존 00~06이 AI 위임 실행 루프(Phase B)에만 강하고 기획(Phase A 앞단)/운영(Phase C 뒷단) 레이어가 비어있음을 발견 |
| v1.2 | 2026-09-05 | 실패 패턴 라이브러리에 브랜치/병합 관련 패턴 다수 추가(부분 병합, dangling commit 소실 위험, 기준 브랜치 불일치 등) + PR 템플릿(`.github/pull_request_template.md`) 추가 + CI에 `repo-health` 잡 추가(.gitignore 존재/빌드산출물 미커밋/부분병합 방지 자동 검증, `harness_19`가 다루지 않던 "저장소 상태" 검증 보완) + 1인 프로젝트용 RACI 대체 습관(§4)·규모별 적용 프로파일(`harness_19` §4) 추가 | 실전 프로젝트 적용 중 실행 강제력 공백(문서엔 있으나 시스템으로 강제되지 않던 규칙들)이 다수 발견되어 CI/템플릿으로 보강. 상세 사례는 도메인 특정 정보라 이 저장소엔 남기지 않음(원칙: 이 표는 항상 일반화된 교훈만 유지) |
| v1.3 | 2026-09-07 | `harness_09`에 §4 DB 마이그레이션 절차(코드 롤백과 스키마 롤백 분리) 추가 + `harness_20_backup_dr.md`(백업/재해복구 정책, RPO/RTO, DR훈련) 신설 + `harness_13`에 §7 의존성/공급망 보안 추가 및 CI에 `dependency-scan` 잡 추가 + `harness_10`에 §6 개인정보 유출사고 대응 절차(법정 신고기한 트랙 분리) 추가 + `harness_11 RACI`에 "백업 복구 실행 승인"·"개인정보 유출 신고 승인" 행 추가 + 관련 색인 문서(`harness_00_definition`, `harness_structure_diagram`, `harness_19`, `README`, `CLAUDE.md`) 전체 21종으로 갱신 | 20년차 기획/설계/개발 관점 갭 분석(2026-09-07) — 배포(merge) 이후 운영 단계 중 "스키마 롤백", "데이터 손실 복구", "공급망 취약점", "개인정보 유출 신고"에 대한 절차가 완전히 비어있었음을 발견. 나머지 발견 사항(🟡/🟢 등급)은 즉시 문서화하지 않고 기존 방침대로 실전에서 부딪히는 대로 이 회고 루프에 흡수하기로 결정 |
| v1.4 | 2026-09-07 | `harness_11`에 §5 기획-설계(PO-TL) 이견 조정 절차(타이브레이크 원칙+에스컬레이션) 추가 + `harness_00_overview`에 §7 실행단위(unit) 크기 산정 기준 추가 + `harness_09`에 §8 포스트모템 템플릿 추가 + `harness_19` §1 필수필드 표에 배포/RACI/변경관리/병렬작업 행 추가 및 🟢 등급 제외 범위 명시, §3에 AC 모호성 키워드 검출 가이드 추가 + `harness_00_definition` §5의 "구조적으로 완성" 표현을 living document 톤으로 수정 + `harness_13` §4에 정적분석/포맷팅/타입체크 정책 추가, CI에 `lint` 잡 추가, PR 템플릿 체크리스트 문구를 실제 CI 잡 구성에 맞춰 갱신 + `harness_04`에 에이전트/모델 버전 변경 시 회귀점검 절차 추가, `harness_05 §5`에 모델 버전 로깅 항목 추가 | v1.3 갭 분석에서 함께 발견됐던 🟡/🟢 항목 중 프로그레시브 롤아웃/피처플래그(브랜치 전략과 결부된 항목이라 Git 정책 결정 보류 중 — 진행 보류)를 제외한 8건을 반영 |

## 2. 프로젝트 회고 템플릿 (프로젝트 종료 시 또는 주요 마일스톤마다 작성)

### §1. 무엇이 잘 작동했는가
- 하네스 요소 중 실제로 시간을 절약하거나 실수를 막아준 것:

### §2. 무엇이 번거로웠거나 안 지켜졌는가
- 문서 필드 중 실제로 안 채워지거나 형식적으로만 채워진 것:
- 규칙 중 실제로는 지켜지지 않고 넘어간 것 (왜 그랬는지도):

### §3. 하네스 문서/규칙 개선 제안
| 대상 문서 | 문제 | 개선안 |
|---|---|---|

### §4. 이번 프로젝트에서 발견된 실패 패턴 (아래 3번 라이브러리에 반영)

---

## 3. 실패 패턴 라이브러리

> 개별 실수가 아니라 "반복되는" 실수만 여기에 등록합니다.
> 새 프로젝트 시작 시 이 목록을 먼저 훑고, 해당하는 것은 TRD/작업지시서 체크리스트에
> 선제적으로 반영합니다.

| 패턴 | 설명 | 재발 방지책 |
|---|---|---|
| 스펙-실제 불일치 | 문서화된 스펙(레거시 코드 분석, 요구사항 문서 등)과 실제 구현 대상이 다름 | 에이전트에게 "원본을 직접 재검증"을 원칙으로 강제 (execution_infra §4 에스컬레이션 프로토콜 연동) |
| 암묵적 완료 가정 | "이건 이미 다른 데서 구현됐을 것"이라고 가정하고 넘어갔다가 실제로 없었음 | 작업지시서 §0 전제조건에서, 의존하는 선행 기능의 실제 코드 위치를 명시하고 확인하게 함 |
| 문서 유형 혼동 | TRD를 복사해서 작업지시서인 척 제출 | 00_overview의 파일명 규칙 + 템플릿 헤더로 구분 강제 |
| 파일명 인코딩 문제 | 한글/공백 섞인 파일명이 특정 환경에서 안 열림 | 파일명 규칙(영문/숫자)을 00_overview에 고정 |
| ADR 대상 결정이 기록 없이 그냥 진행됨 | 삭제정책/동시성처리/인증방식 등 `harness_08` 체크리스트에 명백히 해당하는 결정이 요구사항서·TRD 안에는 있지만 ADR로는 하나도 안 남아 있었음 | 단위 TRD 확정 직후 "이 중 ADR 체크리스트에 해당하는 게 있는가"를 작업지시서 §0에서 한 번 더 확인하는 절차 추가 검토. 발견 즉시 소급 작성(`docs/{project}_adr.md`)으로 만회 가능 |
| 문서 간 상호참조 깨짐 | 마스터TRD/작업지시서가 아직 존재하지 않는 문서를 참조 — 파일을 만들기로 "말"만 하고 실제로 안 만듦 | 문서에 다른 파일 경로를 적을 때, 그 자리에서 파일이 실제로 존재하는지 확인하는 습관화. `harness_19` 템플릿 검증 스크립트가 생기면 깨진 링크 검사도 포함 |
| 보안 정책이 기술스택 결정보다 늦게 채워짐 | Phase A 순서(기술컨벤션 먼저 확정 → 단위 TRD)를 실제로는 안 지키고, 단위 TRD에서 인증/세션 방식까지 먼저 확정된 뒤에야 전역 기술컨벤션이 비어있다는 게 드러남. 그 결과 보안 속성(CSRF, 쿠키 플래그 등) 같은 정책이 구현 직전까지 빠진 채로 방치될 뻔함 | 단위 TRD에서 인증/세션 관련 결정이 나오면, 그 자리에서 즉시 전역 기술컨벤션 문서에 소급 반영하고 관련 보안 속성을 짝지어 검토하는 것을 습관화 |
| PR 부분 병합 | "구현 완료" PR이 실제로는 TRD/작업지시서 문서만 병합되고 소스코드 커밋은 같은 브랜치에 계속 쌓이기만 한 채 base로 병합되지 않음 | 병합 전 `git merge-base --is-ancestor <base> <feature-branch>` 역방향 확인을 머지 체크리스트에 추가; GitHub 브랜치 보호 규칙(리뷰 필수 + 상태 체크)으로 "일부만 병합된 PR"을 구조적으로 차단 |
| .gitignore 부재로 인한 오염 | 브랜치를 로컬에서 오가며 생긴 빌드 산출물(캐시/컴파일 결과물 등)이 `.gitignore` 부재로 base 브랜치에 그대로 커밋됨 — base엔 산출물만 있고 원본 소스는 없어 "코드 실종"처럼 보이는 원인이 됨 | 프로젝트 착수 즉시(harness_05 실행 전) 스택에 맞는 `.gitignore`부터 커밋; 커밋 전 `git status`로 스테이징 내용 diff 확인을 습관화 |
| 정책 확정 순서 역전 | 데이터모델(컬럼)은 이미 "확정"됐는데 그 컬럼이 다루는 민감정보의 생애주기/마스킹 정책은 여전히 "미결"로 남아, 되돌리기 어려운 결정(CLAUDE.md 원칙 8)이 사실상 먼저 굳어짐 | A0 데이터모델 TRD에 PII 성격 컬럼이 포함되면, `harness_10_data_lifecycle.md` 정책 확정을 데이터모델 "확정" 전 선행 조건으로 강제 |
| 미병합 브랜치 임의 삭제로 dangling commit 소실 위험 | 미병합 상태로 확인됐던 브랜치가 시간이 지나 원격에서 아예 삭제되어 있던 사례 — `git fsck`로 dangling commit을 겨우 발견해 복구. `git gc` 타이밍에 따라 영구 소실될 수도 있었음 | "병합 확인 전 브랜치 삭제 금지"를 머지 체크리스트에 명시; GitHub 저장소에서 "Automatically delete head branches"를 끄거나, 최소한 병합 확인 없이 삭제하지 않는 것을 절차로 강제 |
| 기준 브랜치 선언과 실제 운영 불일치 | CLAUDE.md가 기준 브랜치를 A로 선언했지만, 실제로는 B에서 작업해 A는 B를 머지만 받는 하위 브랜치로 쓰임 — 그 결과 선언상의 기준 브랜치엔 실제 산출물이 하나도 없음 | 프로젝트 착수 시 기준 브랜치를 딱 하나만 선언하고, 실제 `git push`/PR 대상이 그 선언과 일치하는지 매 PR마다 확인 (자동화하려면 CI에서 PR 대상 브랜치가 문서상 기준 브랜치와 동일한지 체크) |
| Phase A 착수 시 요약본만 읽고 원문 SOP·실패패턴 라이브러리를 건너뜀 | `harness_00_definition.md`(5분 요약)만 읽고 Phase A(ADR/마스터TRD/릴리스계획)를 진행 — 원문 SOP(`harness_00_overview.md`)의 §5 파일명 규칙, `harness_05`의 "기준 브랜치 선언=CI 대상 일치" 요구, 그리고 이 표(§3)에 이미 등록돼 있던 "파일명 인코딩 문제"·"기준 브랜치 선언과 실제 운영 불일치" 패턴을 몰라서 **이미 알려진 실수를 그대로 재현**함(마스터 TRD 파일명에 프로젝트코드 누락, CI 브랜치가 실제 기준 브랜치와 불일치) | Phase A 착수 직전 체크리스트에 "①`harness_00_overview.md` 원문 전독, ②이 실패 패턴 표(§3) 전체를 훑고 해당 항목을 TRD/작업지시서 체크리스트에 선제 반영"을 고정 단계로 강제. 요약 문서(`00_definition`)는 지도용일 뿐 원문을 대체하지 않는다는 점을 그 문서 자체에도 명시 검토 |

(프로젝트를 진행하면서 이 표는 계속 늘어납니다. 이게 이 계층의 존재 이유입니다.)

## 4. 메타 회고 주기

- 단위(화면) 완료마다: A0 인수인계 문서 갱신 (실행 계층)
- 프로젝트 완료마다: 이 문서의 §2 회고 템플릿 작성 + §1 하네스 버전 갱신
- 3~5개 프로젝트마다: 실패 패턴 라이브러리를 다시 훑어서, 자주 등장하는 것은
  아예 프롬프트 생성기(04)의 "블록 1 — 역할과 원칙"에 상시 규칙으로 승격
