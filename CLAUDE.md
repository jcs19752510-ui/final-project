# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 세션을 시작할 때마다 자동으로 읽는
지속 지침 파일입니다. 절대 임의로 무시하지 마세요.

## ⚠️ Git 관련 작업 금지 (2026-09-05, 사용자 지시 — 최우선 규칙)

**Claude는 이 저장소에서 git과 관련된 어떠한 작업도 스스로 수행하지 않는다.**
`git status`/`diff`/`log` 같은 조회성 명령을 포함해 `add`/`commit`/`merge`/
`branch`/`push`/`rm` 등 상태를 바꾸는 명령까지 전부 금지 대상이다. 브랜치
생성, 커밋, 병합, 파일 삭제 등 지금까지 이 세션에서 했던 모든 git 조작
방식은 앞으로 전면 중단한다. git으로 확인해야 할 정보가 필요하면 사용자에게
직접 실행해달라고 요청하고 결과를 받아서 참고한다. 파일 내용 편집 자체
(Read/Edit/Write)는 이 규칙의 대상이 아니다 — git 명령 실행만 금지다.

## 프로젝트 개요
- 이 저장소(`HARNESS_BASIC`)는 **특정 업무 프로그램(예: 출결관리, 재고관리,
  예약시스템 등)의 구현체가 아닙니다.** AI에게 개발을 위임할 때 다른 모든
  프로그램이 공통으로 가져다 쓸 **범용 거버넌스 프레임워크(하네스 시스템)
  그 자체**를 관리하는 저장소입니다.
- `harness/` 아래 21종 문서가 이 저장소의 본체입니다. `docs/`, `src/`,
  `tests/` 등은 실제 프로젝트가 이 하네스를 가져다 쓸 때 채우는 **빈 골격
  (스켈레톤)**이며, 이 저장소 자체에는 도메인 특정 내용을 채우지 않습니다
  (harness_00_definition.md 상단 경고 참조).
- 과거 세션에서 특정 애플리케이션(예시 프로젝트) 코드/문서를 이 저장소에
  직접 채워넣었다가, 하네스 자체의 범용성을 해친다는 문제가 발견되어
  2026-09-05에 전부 제거하고 골격만 남겼습니다 — 같은 실수를 반복하지
  마세요: **이 저장소에는 특정 프로그램의 TRD/코드/테스트를 커밋하지 않습니다.**
- 사람이 처음 방문했을 때 보는 진입점은 루트 `README.md`입니다. 이 파일과
  내용이 어긋나면 안 되므로, 아래 원칙/구조를 바꿀 때는 `README.md`도
  함께 갱신하세요.

## 저장소 연결 방식 — 이 저장소를 실제 프로젝트에 어떻게 쓰는가 (2026-09-05 결정)

**결정**: 이 저장소는 GitHub **Template repository**로 사용한다 (사람이
Settings → General에서 최초 1회 체크). 새 프로젝트를 시작할 때마다 "Use
this template" 버튼으로 완전히 새로운 저장소를 만들고, 그 새 저장소
안에서만 실제 코드/TRD/문서를 쌓는다. 이 저장소(`HARNESS_BASIC`) 자체는
영원히 빈 골격만 유지한다.

**검토했던 대안과 기각 이유**:
- 매번 `harness/` 폴더만 수동 복사 → 간단하지만 버전 추적이 안 되고,
  이 저장소가 개선돼도 이미 복사된 프로젝트에 반영이 안 됨
- git submodule/subtree로 여러 프로젝트가 참조 → 복잡도 대비 이득이
  적음(1인/소규모 프로젝트엔 과함)

새 저장소 생성 후 할 일: 그 저장소의 `CLAUDE.md` "프로젝트 개요"를 실제
프로젝트 설명으로 교체(하네스 원칙 8개와 이 "Git 관련 작업 금지" 규칙은
그대로 유지), 이후 `harness_00_definition.md` §4 Phase A부터 순서대로
진행. 같은 내용이 `README.md`와 `harness_00_definition.md` 양쪽에도
동일하게 안내돼 있습니다.

## 하네스 원칙 (harness/harness_00_overview.md 기반, 반드시 준수)

1. **스코프 준수**: `docs/trd/`에 없는 기능을 임의로 추가하지 않는다.
2. **에스컬레이션**: 정책이 불명확하면 임의로 판단하지 말고, 진행을 멈추고
   구체적으로 무엇이 불명확한지 보고한다. (예외: 작업지시서 §3에 기본값이
   명시된 항목은 그 기본값을 따라 진행)
3. **선행 확인**: 작업 시작 전 반드시 해당 단위의 TRD(`docs/trd/`)와
   작업지시서(`docs/workorder/`)를 먼저 읽는다.
4. **자기 검증**: 작업 완료 후 TRD의 AC(§5) 각 항목에 대해 스스로
   pass/fail을 점검하고 결과를 보고한다.
5. **판단 근거 기록**: 스펙에 명시 안 된 세부사항을 채웠다면, 왜 그렇게
   채웠는지 근거를 결과 보고에 포함한다.
6. **원본 우선**: 스펙과 실제 코드/데이터가 다르면 실제 쪽을 우선하고,
   이 사실을 반드시 보고한다.
7. **개인정보 보호**: 이용자/고객 등 실제 개인정보는 실제 값 대신 항상
   샘플/마스킹 데이터만 사용한다. 실제 개인정보를 코드나 시드 데이터에
   하드코딩하지 않는다.
8. **되돌리기 어려운 결정 금지**: 데이터 모델 변경, 삭제 정책(물리삭제 등),
   권한 구조는 스스로 확정하지 말고 후보만 제시한 뒤 사람의 승인을 받는다.

## Git 워크플로우 (이 하네스를 실제 프로젝트에 적용할 때의 권장 방식)
- 프로젝트 착수 시 기준(base) 브랜치를 하나만 정하고 CLAUDE.md에 명시한다 —
  선언한 기준 브랜치와 실제 push/PR 대상이 항상 일치하는지 확인한다
  (`harness_06_meta_improvement.md` §3 "기준 브랜치 선언과 실제 운영 불일치" 참조).
- 기준 브랜치에는 직접 커밋하지 않는다.
- 작업 단위마다 `feature/{단위코드}` 브랜치를 새로 만들어 작업한다.
- 커밋 메시지 형식: `[{단위코드}] {한 일 요약}`
- 브랜치는 base에 실제로 병합된 것을 확인(`git merge-base --is-ancestor`)한
  뒤에만 삭제한다 (`.github/pull_request_template.md` 체크리스트 참조).

## 참고 문서 경로

### 기본 문서 (00~06)
- **처음 읽는 5분 요약(전체 지도, 머메이드)**: `harness/harness_00_definition.md`
- 전체 SOP 및 타협 불가 규칙: `harness/harness_00_overview.md`
- TRD 템플릿: `harness/harness_01_trd_template.md`
- 작업지시서 템플릿: `harness/harness_02_work_order_template.md`
- 인수인계(A0) 템플릿: `harness/harness_03_handoff_template.md`
- 프롬프트 생성 규칙: `harness/harness_04_prompt_generator_template.md`
- 실행 인프라(브랜치/CI/권한/에스컬레이션): `harness/harness_05_execution_infra.md`
- 회고 및 실패 패턴 라이브러리: `harness/harness_06_meta_improvement.md`

### 확장 문서 (v1.1 — 2026-09-04 갭 분석으로 추가)
- 🔴 릴리스/우선순위 계획: `harness/harness_07_release_planning.md`
- 🔴 아키텍처 결정 기록(ADR): `harness/harness_08_adr.md`
- 🔴 배포/롤백 절차: `harness/harness_09_deployment_runbook.md`
- 🔴 개인정보 생애주기 정책: `harness/harness_10_data_lifecycle.md`
- 🔴 백업/재해복구 정책: `harness/harness_20_backup_dr.md` (2026-09-07 추가)
- 🟡 역할과 책임(RACI): `harness/harness_11_raci.md`
- 🟡 변경관리 프로세스: `harness/harness_12_change_management.md`
- 🟡 전역 기술 컨벤션: `harness/harness_13_tech_conventions.md`
- 🟡 테스트 데이터/시드 관리: `harness/harness_14_test_data_management.md`
- 🟡 병렬 작업/인터페이스 계약 관리: `harness/harness_15_parallel_work.md`
- 🟢 기술 부채 로그: `harness/harness_16_tech_debt_log.md`
- 🟢 모니터링/알림 기준: `harness/harness_17_monitoring.md`
- 🟢 AI 세션 비용/시간 추적: `harness/harness_18_cost_tracking.md`
- 🟢 템플릿 검증 체크: `harness/harness_19_template_validation.md`

## 확장 문서(07~20)는 왜 있어야 하는가 — 20년차 기획/설계/개발 관점

> 기본 문서(00~06)는 "AI에게 무엇을 어디까지 시킬지"를 다룹니다. 하지만 실제
> 프로젝트는 그 앞(기획)과 뒤(운영)가 반드시 붙습니다. 아래는 각 문서가 없을 때
> 실제로 터지는 문제를 근거로 설명한 것입니다 — "있으면 좋다"가 아니라
> "없으면 이렇게 됩니다"로 읽어주세요.

### 🔴 Critical (없으면 실제 프로젝트에서 반드시 사고가 남)

**`harness_07_release_planning.md` — 릴리스/우선순위 계획**
기존 SOP(`harness_00`)는 "위험도" 하나만으로 실행 순서를 정합니다. 그런데
위험도와 비즈니스 가치는 서로 다른 축입니다. 위험도만 보고 순서를 짜면 정작
급하게 써야 할 핵심 기능보다 위험한 부수 기능이 먼저 나오는
역전이 생깁니다. 20년차 PM이라면 반드시 MoSCoW/MVP 범위부터 확정하고 그
위에 위험도를 얹습니다 — 순서가 바뀌면 "다 만들었는데 정작 써야 할 건 마지막"
이라는 전형적인 실패가 재현됩니다.

**`harness_08_adr.md` — 아키텍처 결정 기록**
`harness_00 §4-1`은 "되돌리기 어려운 결정은 AI가 혼자 확정하지 못한다"고
못박아 놓고도, 정작 그 결정을 **어디에 기록하는지**는 정의하지 않았습니다.
기록이 없으면 3개월 뒤 "이 테이블을 왜 이렇게 정규화했지?", "왜 세션 방식이
아니라 토큰 방식을 골랐지?"에 아무도 답을 못하고, 새로 온 사람(혹은 새 AI
세션)이 같은 논의를 반복하거나 실수로 이미 폐기된 대안으로 되돌아갑니다.
설계자에게 ADR은 "코드 리뷰만큼 중요한 결정 리뷰"입니다.

**`harness_09_deployment_runbook.md` — 배포/롤백 절차**
`harness_05`는 정확히 "merge까지"만 다루고 끝납니다. merge된 코드가 실제
사용자에게 닿는 경로, 그리고 문제가 터졌을 때 되돌리는 절차가 없으면 두 가지
사고가 동시에 날 수 있습니다: (1) 코드는 완성됐는데 아무도 배포를 못 해서
방치되거나, (2) 급하게 즉흥적으로 배포하다가 장애가 나도 롤백 기준이 없어서
사태를 키웁니다. 20년차 엔지니어에게 "머지 = 완료"가 아니라 "머지는 배포의
시작"입니다.

**`harness_10_data_lifecycle.md` — 개인정보 생애주기 정책**
CLAUDE.md 원칙 7번("개인정보 보호")과 `harness_05 §3`은 **개발 단계**의
마스킹만 다룹니다. 정작 실제 운영 데이터베이스에 쌓이는 이용자/고객 개인정보를
언제까지 보관하고, 탈퇴/계약종료 시 언제 파기하며, 당사자가 열람·정정을 요청하면
어떻게 처리하는지는 완전히 비어 있었습니다. 개인정보는 법적 규제 대상이고,
이 공백은 기능 버그가 아니라 **법적 리스크**입니다. 그래서 이 문서는
`harness_00 §4-8`("되돌리기 어려운 결정 금지")의 적용 대상으로 명시했고, AI가
값을 채우지 못하도록 곳곳에 경고를 넣었습니다.

**`harness_20_backup_dr.md` — 백업/재해복구 정책**
`harness_10 §3` 파기 절차는 "관련 백업본 파기 일정 확인"이라며 백업의 *존재*를
전제로 쓰였지만, 정작 백업을 얼마나 자주 만들고 얼마나 보관하며 실제로
복구가 되는지 검증하는 절차는 어디에도 없었습니다. 백업은 만들어두는 것과
복구가 되는 것이 다른 문제입니다 — 복구 테스트를 한 번도 안 해본 백업은
장애 시점에야 손상됐다는 걸 알게 되는 경우가 흔합니다. 또한 코드 롤백
(`harness_09`)과 스키마 마이그레이션 롤백은 별개 문제라서, 이 문서가 없으면
"배포는 롤백했는데 DB는 계속 깨져 있다"는 사고가 반복됩니다. RPO/RTO 목표치는
`harness_00 §4-1`의 "되돌리기 어려운 결정" 적용 대상으로 명시했습니다.

### 🟡 Important (프로젝트/팀 규모가 커지면 반드시 아쉬워짐)

**`harness_11_raci.md` — 역할과 책임**
1인 프로젝트에서는 "누가 승인하나"가 안 보입니다 — 본인이 다 하니까요. 하지만
이 하네스를 다른 프로젝트, 다른 팀에 이식하는 순간 "TRD는 누가 최종 확정하고,
merge는 누가 승인하는가"가 불명확해지면 하네스 전체가 형식적인 서류로
전락합니다. 20년차 PM은 항상 "이 결정, 누구 책임이야?"를 먼저 묻습니다 —
이게 없으면 책임소재가 애매해질 때마다 프로세스가 아니라 사람 목소리 큰 쪽이
이깁니다.

**`harness_12_change_management.md` — 변경관리 프로세스**
`harness_00 §4-7`은 "스펙 문서는 단일 진실 공급원을 유지한다"고 선언만
합니다. 확정된 TRD를 실제로 바꿔야 할 때(요구사항 변경은 반드시 옵니다)
누가 승인하고, 이미 완료된 단위에 미치는 영향을 어떻게 분석하는지가 없으면,
결국 채팅으로 "이거 이렇게 바꿔주세요"가 스펙이 되고 문서는 낡습니다.
설계자 관점에서 이건 "스펙 드리프트"의 가장 흔한 원인입니다.

**`harness_13_tech_conventions.md` — 전역 기술 컨벤션**
각 단위 TRD의 §2(API 명세)는 단위별로 따로 정의됩니다. 전역 표준이 없으면
AI 세션마다, 사람마다 에러 응답 포맷/네이밍/인증 방식이 미묘하게 달라지고,
나중에 프론트와 백엔드를 조립하는 시점에 "부품이 안 맞는" 사고가 납니다.
20년차 개발자는 이걸 프로젝트 착수 시점에 딱 한 번 고정하고 그 뒤로는
협상하지 않는 것을 원칙으로 삼습니다.

**`harness_14_test_data_management.md` — 테스트 데이터/시드 관리**
`harness_05 §3`은 "마스킹 시드 데이터를 준비하라"고만 하고 그 시드를 누가
만들고 어디 있고 어떻게 재사용하는지는 정의하지 않았습니다. 이게 없으면
단위마다 계정을 새로 만들어 로그인 정보가 꼬이거나, 같은 테스트 대상인데
단위별로 다른 값을 써서 통합 테스트에서 앞뒤가 안 맞는 상황이 반드시 생깁니다.

**`harness_15_parallel_work.md` — 병렬 작업/인터페이스 계약 관리**
현재 SOP는 단위를 하나씩 순차로 진행하는 것을 전제로 합니다. 프로젝트가
커지면 서로 의존관계 없는 단위를 동시에 여러 세션으로 돌리고 싶어지는데,
인터페이스가 문서로 고정돼 있지 않으면 한쪽이 API를 바꿔도 다른 쪽은
그걸 모른 채 진행하다가 통합 시점에 깨집니다. 설계자에게 이건 고전적인
"팀 간 계약(contract) 관리" 문제이고, AI 멀티세션 환경에서는 더 빨리 터집니다.

### 🟢 Nice-to-have (있으면 하네스가 더 단단해짐)

**`harness_16_tech_debt_log.md` — 기술 부채 로그**
A0 인수인계 §3(편차)는 "몰랐는데 다르게 나온 것"을 기록하는 곳이라, "알면서
의도적으로 미룬 것"을 여기에 섞으면 문서가 지저분해지고 미룬 일들이 시간이
지나면 잊힙니다. 20년차 개발자는 기술부채를 "감정이 아니라 목록"으로
관리해야 나중에 실제로 갚는다는 걸 압니다.

**`harness_17_monitoring.md` — 모니터링/알림 기준**
`harness_05 §5`의 관찰가능성은 개발 단계 로그(에이전트가 뭘 했는지)만
다룹니다. 운영 중 시스템이 실제로 죽었는지, 느려졌는지, 개인정보에 이상
접근이 있었는지는 완전히 다른 문제이고, 이게 없으면 사용자 신고가 유일한
장애 감지 수단이 됩니다.

**`harness_18_cost_tracking.md` — AI 세션 비용/시간 추적**
`harness_06` 회고는 "시간을 절약했는가"를 정성적으로만 묻습니다. 하네스가
실제로 효율적인지, 어느 단계에서 재시도가 몰려 비용이 새는지는 숫자 없이는
판단이 안 되고, 숫자가 없으면 하네스 개선도 감으로만 하게 됩니다.

**`harness_19_template_validation.md` — 템플릿 검증 체크**
지금 모든 문서는 사람이 체크리스트를 눈으로 보고 스스로 확인하는 방식입니다.
바쁘면 빈칸을 건너뛰고 다음 단계로 넘어가기 쉽습니다. 이 문서는 각 문서
유형별 필수 필드를 명시해서, 나중에 간단한 스크립트로도 기계 검증할 수
있는 형태로 정리해 둔 것입니다.

## 알려진 실패 패턴 (harness_06 §3 연동 — 새로 발견되면 여기와 그 파일에 함께 추가)

이 저장소 자체는 도메인 중립을 유지해야 하므로, 특정 프로젝트에서 실제로
겪은 사고의 상세 서술(날짜/프로젝트명/구체적 파일명 등)은 여기 남기지 않고
`harness/harness_06_meta_improvement.md` §3 "실패 패턴 라이브러리"에 **일반화된
형태**(구체 사례를 제거하고 재사용 가능한 교훈만)로만 누적합니다. 새 패턴을
발견하면 반드시 그 형태(패턴명/설명/재발방지책, 프로젝트 식별 정보 없이)로
그 표에 추가하세요.